#include<bits/stdc++.h>
using namespace std;

int main()
{
	/*int diff,t,i,n,c=0,ans=-1,tmp,j=1;
	cin>>t;
	while(t--){
		cin>>n;
		ans=-1;
		vector <int> arr(n);
		for(i=0;i<n;i++)
			cin>>arr[i];
		diff=arr[1]-arr[0];
		c=2;
		for(i=2;i<n;i++){
			tmp=arr[i]-arr[i-1];
			if(tmp!=diff){
				ans=max(ans,c);
				diff=tmp;
				c=2;
			}
			else{
				c++;
			}
		}
		if(ans < c)
		    ans=c;
		//ans=max(ans,c);
		cout<<"Case #"<<j<<": "<<ans<<endl;
		j++;
	}*/
	char ch;
	while(1){
		cin>>ch;
		cout<<int(ch)<<endl;
	}
	return 1;
}
